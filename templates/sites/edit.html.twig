{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}: Edit sites {% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un site</h1>

        <div>
            <label for="edit">Nom du site</label>
            <input type="text" id="edit" name="edit" placeholder="{{ detail.nom }}"/>
            <button type="submit">Enregistrer</button>
        </div>
    </div>

{% endblock %}